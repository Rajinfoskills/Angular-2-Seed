import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'seed-app',
    templateUrl: './views/app.component.html'
})
export class AppComponent {
    title: string = "Welcome to Angular 2 World";
}

